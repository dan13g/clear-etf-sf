select
    asset_key,
    ticker,
    etf_code,
    isin,
    fund_name,
    provider_name,
    index_code,
    index_name,
    equivalence_group_code,
    relationship_to_group,
    asset_class,
    category,
    distribution_type,
    replication_method,
    currency,
    domicile,
    hedged_flag,
    ucits_flag,
    ter,
    inception_date,
    is_active,
    notes
from {{ ref('stg_etf_metadata') }}

select
    cast(etf as varchar) as etf_code,
    cast(ticker as varchar) as ticker,
    cast(isin as varchar) as isin,
    cast(fund_name as varchar) as fund_name,
    cast(provider as varchar) as provider_name,
    cast("index" as varchar) as index_code,
    cast(equivalence_group_code as varchar) as equivalence_group_code,
    cast(relationship_to_group as varchar) as relationship_to_group,
    cast(asset_class as varchar) as asset_class,
    cast(category as varchar) as category,
    cast(distribution_type as varchar) as distribution_type,
    cast(replication_method as varchar) as replication_method,
    cast(currency as varchar) as currency,
    cast(domicile as varchar) as domicile,
    cast(hedged_flag as varchar) as hedged_flag,
    cast(ucits_flag as varchar) as ucits_flag,
    cast(ter as double) as ter,
    cast(inception_date as date) as inception_date,
    cast(is_active as varchar) as is_active,
    cast(notes as varchar) as notes
from {{ source('motherduck_raw', 'etf') }}

name: "clear_etf"
version: "3.0.0"
config-version: 2

profile: "clear_etf"

model-paths: ["models"]
macro-paths: ["macros"]

models:
  clear_etf:
    staging:
      +materialized: table
      +schema: stg
    dimensions:
      +materialized: table
      +schema: dimensions
    intermediate:
      +materialized: table
      +schema: int
    marts:
      +materialized: table
      +schema: marts
    exports:
      +materialized: table
      +schema: exports
    reports:
      +materialized: table
      +schema: reports

on-run-start:
